<UserControl x:Class="BirdsWPF.Views.UserControls.AddSpecieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BirdsWPF.Views.UserControls"
             xmlns:vm="clr-namespace:BirdsViewModels;assembly=BirdsViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:AddSpecieViewModel}">
    <UniformGrid Columns="1">
        <TextBox VerticalAlignment="Center" HorizontalAlignment="Center"
                 MinWidth="150"
                 Text="{Binding Name}"/>
        <Button Margin="5" Padding="15 5" VerticalAlignment="Center" HorizontalAlignment="Center"
                Content="Добавить вид"
                Command="{Binding AddSpecieCommand, Mode=OneWay, Source={StaticResource mainVM}}"
                CommandParameter="{Binding Name}"/>
    </UniformGrid>
</UserControl>
